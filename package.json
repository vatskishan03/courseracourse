{
  "name": "courseraproject-with-link",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "npm install && npm install parcel-bundler --save-dev && npx parcel build public/index.html",
    "vercel": "vercel",
    "postinstall": "cd api && npm install"
  },
  "dependencies": {
    "express": "^4.17.1",
    "body-parser": "^1.19.0",
    "csv-parser": "^3.0.0",
    "python-shell": "*"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
